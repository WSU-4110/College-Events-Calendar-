
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:actionViewClass="android.support.v7.widget.SearchView"
    android:id="@+id/menu_main"
    android:fontFamily="@font/blacklist"
    >
    <item
        android:id="@+id/newEvent"
        android:icon="@drawable/addingeventpic"
        app:showAsAction="ifRoom"
        android:title="ADD EVENT">
    </item>

    <item
        android:id="@+id/search"
        android:icon="@drawable/search"
        app:showAsAction="collapseActionView|ifRoom"
        android:title="SEARCH EVENT"
        app:actionViewClass="android.widget.SearchView"
        >
    </item>

    <!-- OUR MENU GOES HERE, WHAT DO WE WANT THEM TO DO HERE? -->
    <!-- MAYBE LOG-IN OR LOG-OUT? SWITCH BETWEEN PAGES?-->

    <item
        android:id="@+id/login"
        android:title="Login"
        />
    <item
        android:id="@+id/logout"
        android:title="Logout"/>

</menu>